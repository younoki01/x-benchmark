name: Keep Alive

on:
  schedule:
    - cron: '*/10 * * * *'  # 10分ごと

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render
        run: curl https://x-post-bot-23kn.onrender.com/